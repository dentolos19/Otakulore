<Page x:Class="Otakulore.Views.DetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:xi="using:Microsoft.Xaml.Interactivity"
      xmlns:xic="using:Microsoft.Xaml.Interactions.Core"
      xmlns:uic="using:CommunityToolkit.WinUI.UI.Controls"
      xmlns:olm="using:Otakulore.Models"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <olm:DetailsViewModel/>
    </Page.DataContext>
    <ScrollViewer>
        <Grid Margin="10" RowSpacing="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <uic:ImageEx Grid.Column="0" Source="{Binding ImageUrl}" CornerRadius="10"/>
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <CommandBar Grid.Row="0" OverflowButtonVisibility="Collapsed">
                        <CommandBar.PrimaryCommands>
                            <AppBarButton ToolTipService.ToolTip="Play">
                                <AppBarButton.Icon>
                                    <FontIcon Glyph="&#xE768;"/>
                                </AppBarButton.Icon>
                                <AppBarButton.Flyout>
                                    <Flyout>
                                        <GridView x:Name="ProviderList" ItemTemplate="{StaticResource ProviderItemTemplate}" IsItemClickEnabled="True" ItemClick="OnProviderClicked"/>
                                    </Flyout>
                                </AppBarButton.Flyout>
                            </AppBarButton>
                            <AppBarButton ToolTipService.ToolTip="Track" Click="OnTrackRequested">
                                <AppBarButton.Icon>
                                    <FontIcon Glyph="&#xE895;"/>
                                </AppBarButton.Icon>
                            </AppBarButton>
                            <AppBarToggleButton ToolTipService.ToolTip="Favorite" IsChecked="{Binding IsFavorite, Mode=TwoWay}" Click="OnUpdateFavorite">
                                <AppBarToggleButton.Icon>
                                    <FontIcon Glyph="&#xE734;"/>
                                </AppBarToggleButton.Icon>
                            </AppBarToggleButton>
                        </CommandBar.PrimaryCommands>
                    </CommandBar>
                    <TextBlock Grid.Row="2" Text="{Binding Title}" FontWeight="Bold" FontSize="24"/>
                    <TextBlock Grid.Row="3" Text="{Binding Subtitle}"/>
                </Grid>
            </Grid>
            <Grid Grid.Row="1" ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="10">
                    <StackPanel Spacing="5">
                        <TextBlock Text="Format" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Format, Converter={StaticResource EnumDescriptionConverter}}"/>
                    </StackPanel>
                    <StackPanel Spacing="5">
                        <TextBlock Text="Status" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Status, Converter={StaticResource EnumDescriptionConverter}}"/>
                    </StackPanel>
                    <StackPanel Spacing="5">
                        <TextBlock x:Name="ContentText" FontWeight="Bold">
                            <xi:Interaction.Behaviors>
                                <xic:DataTriggerBehavior Binding="{Binding Type, Converter={StaticResource EnumIntegerConverter}}" Value="0">
                                    <xic:ChangePropertyAction TargetObject="{Binding ElementName=ContentText}" PropertyName="Text" Value="Episodes"/>
                                </xic:DataTriggerBehavior>
                                <xic:DataTriggerBehavior Binding="{Binding Type, Converter={StaticResource EnumIntegerConverter}}" Value="1">
                                    <xic:ChangePropertyAction TargetObject="{Binding ElementName=ContentText}" PropertyName="Text" Value="Chapters"/>
                                </xic:DataTriggerBehavior>
                                <xic:DataTriggerBehavior Binding="{Binding Format, Converter={StaticResource EnumIntegerConverter}}" Value="2">
                                    <xic:ChangePropertyAction TargetObject="{Binding ElementName=ContentText}" PropertyName="Text" Value="Length"/>
                                </xic:DataTriggerBehavior>
                            </xi:Interaction.Behaviors>
                        </TextBlock>
                        <TextBlock Text="{Binding Content}"/>
                    </StackPanel>
                    <StackPanel Spacing="5">
                        <TextBlock Text="Start Date" FontWeight="Bold"/>
                        <TextBlock Text="{Binding StartDate}"/>
                    </StackPanel>
                    <StackPanel Spacing="5">
                        <TextBlock Text="End Date" FontWeight="Bold"/>
                        <TextBlock Text="{Binding EndDate}"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" Spacing="10">
                    <StackPanel Spacing="5">
                        <TextBlock Text="Description" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Description}" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>