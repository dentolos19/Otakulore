<Page x:Class="Otakulore.Views.ContentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:webView="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
      xmlns:models="clr-namespace:Otakulore.Models">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <webView:WebView2 x:Name="WebView" Grid.Row="0" Source="https://dennise.me"/>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="&#xE72B;" FontFamily="Segoe MDL2 Assets"/>
            <ComboBox x:Name="ContentList" Grid.Column="2" SelectionChanged="OnContentSelected">
                <ComboBox.ItemTemplate>
                    <DataTemplate DataType="models:ContentItemModel">
                        <TextBlock Text="{Binding Content.Name}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Grid.Column="4" Content="&#xE72A;" FontFamily="Segoe MDL2 Assets"/>
        </Grid>
    </Grid>
</Page>