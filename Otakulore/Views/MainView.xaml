<Page x:Class="Otakulore.Views.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <NavigationView x:Name="PageNavigation" PaneTitle="Made with ❤️ for Anime" IsPaneOpen="False" IsBackEnabled="{Binding CanGoBack, ElementName=PageFrame}" SelectionChanged="OnNavigatePage" BackRequested="OnBackRequested">
        <NavigationView.AutoSuggestBox>
            <AutoSuggestBox PlaceholderText="Search" QuerySubmitted="OnSearchRequested"/>
        </NavigationView.AutoSuggestBox>
        <Grid>
            <Frame x:Name="PageFrame" x:FieldModifier="internal" CacheSize="5" Navigating="OnPageNavigating"/>
            <controls:InAppNotification x:Name="NotificationArea" x:FieldModifier="internal" ContentTemplate="{StaticResource NotificationContentTemplate}"/>
        </Grid>
    </NavigationView>
</Page>