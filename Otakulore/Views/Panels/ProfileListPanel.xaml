<Page x:Class="Otakulore.Views.Panels.ProfileListPanel"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      DataContext="{Binding RelativeSource={RelativeSource Self}}"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
      NavigationCacheMode="Required">
    <Grid Margin="10" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" ColumnSpacing="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" Grid.Column="0" PlaceholderText="Search" TextChanged="OnSearchChanged"/>
            <ComboBox x:Name="TypeDropdown" Grid.Column="1" SelectionChanged="OnTypeChanged"/>
            <ComboBox x:Name="StatusDropdown" Grid.Column="2" SelectionChanged="OnStatusChanged"/>
        </Grid>
        <ListView x:Name="EntryList" Grid.Row="1" ItemTemplate="{StaticResource MediaItemDetailedTemplate}" IsItemClickEnabled="True" ItemClick="OnItemClicked"/>
        <ProgressRing x:Name="EntryListIndicator" Grid.Row="1"/>
    </Grid>
</Page>