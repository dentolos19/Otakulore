<ContentDialog x:Class="Otakulore.Views.Dialogs.ManageTrackerDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Spacing="10">
        <ComboBox x:Name="StatusDropdown" Header="Status" HorizontalAlignment="Stretch"/>
        <NumberBox x:Name="ProgressBox" Header="Progress" Value="0"/>
        <Grid ColumnSpacing="5" RowSpacing="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Grid.Column="0" Content="Save" HorizontalAlignment="Stretch" Click="OnSave"/>
            <Button Grid.Row="0" Grid.Column="1" Content="Delete" HorizontalAlignment="Stretch" Click="OnDelete"/>
            <Button Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Content="Cancel" HorizontalAlignment="Stretch" Click="OnCancel"/>
        </Grid>
    </StackPanel>
</ContentDialog>