<ContentDialog x:Class="Otakulore.Views.Dialogs.ManageTrackerDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Spacing="10">
        <ComboBox Header="Status" HorizontalAlignment="Stretch"/>
        <NumberBox Header="Progress"/>
        <Grid ColumnSpacing="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Save" HorizontalAlignment="Stretch" Click="OnSave"/>
            <Button Grid.Column="1" Content="Cancel" HorizontalAlignment="Stretch" Click="OnCancel"/>
        </Grid>
    </StackPanel>
</ContentDialog>