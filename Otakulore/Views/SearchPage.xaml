<ui:Page x:Class="Otakulore.Views.SearchPage"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:ui="http://schemas.modernwpf.com/2019"
         xmlns:c="clr-namespace:Otakulore.Core"
         xmlns:m="clr-namespace:Otakulore.Models"
         xmlns:vm="clr-namespace:Otakulore.ViewModels">
    <Page.DataContext>
        <vm:SearchViewModel/>
    </Page.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchInput" Grid.Column="0" KeyDown="OnSearchRequest"/>
            <ComboBox x:Name="CategorySelection" Grid.Column="2" SelectionChanged="OnCategoryChange">
                <ComboBoxItem>Anime</ComboBoxItem>
                <ComboBoxItem>Manga</ComboBoxItem>
            </ComboBox>
        </Grid>
        <ListBox Grid.Row="2" ItemsSource="{Binding Path=SearchItems}">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="m:SearchItemModel">
                    <Grid Height="150">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0" Source="{Binding Path=ImageUrl}"/>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="{Binding Path=Title}" TextTrimming="CharacterEllipsis" FontSize="24"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Path=Year}"/>
                                <TextBlock Text="&#x2022;" Margin="5,0"/>
                                <TextBlock Text="{Binding Path=Contents}"/>
                                <TextBlock Text="&#x2022;" Margin="5,0"/>
                                <TextBlock Text="{Binding Path=Status}"/>
                            </StackPanel>
                            <ui:RatingControl Value="{Binding Path=Score}" HorizontalAlignment="Left" IsReadOnly="True">
                                <ui:RatingControl.Style>
                                    <Style TargetType="ui:RatingControl">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=Score}" Value="-1">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ui:RatingControl.Style>
                            </ui:RatingControl>
                            <TextBlock Text="{Binding Path=Description}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ui:ProgressRing Grid.Row="2" IsActive="True">
            <ui:ProgressRing.Style>
                <Style TargetType="ui:ProgressRing">
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=HasNoSearchResult}" Value="False"/>
                                <Condition Binding="{Binding Path=SearchItems.Count}" Value="0"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible"/>
                        </MultiDataTrigger>
                    </Style.Triggers>
                    <Setter Property="Visibility" Value="Collapsed"/>
                </Style>
            </ui:ProgressRing.Style>
        </ui:ProgressRing>
        <TextBlock Grid.Row="2" Text="Search yielded no results!" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=HasNoSearchResult}" Value="True"/>
                                <Condition Binding="{Binding Path=SearchItems.Count}" Value="0"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible"/>
                        </MultiDataTrigger>
                    </Style.Triggers>
                    <Setter Property="Visibility" Value="Collapsed"/>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</ui:Page>