<Page x:Class="Otakulore.Views.SearchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid Margin="10" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <AutoSuggestBox x:Name="SearchInput" Grid.Column="0" PlaceholderText="Search" QuerySubmitted="OnSearchRequested"/>
            <ComboBox x:Name="SearchTypeSelection" Grid.Column="1" SelectionChanged="OnSearchTypeChanged">
                <ComboBoxItem Content="Anime"/>
                <ComboBoxItem Content="Manga"/>
            </ComboBox>
        </Grid>
        <GridView Grid.Row="1" x:Name="SearchResultList" ItemTemplate="{StaticResource MediaItemTemplate}" IsItemClickEnabled="True" ItemClick="OnItemClicked"/>
    </Grid>
</Page>